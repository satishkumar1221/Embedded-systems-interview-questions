#pragma once
class Node
{
	public :
		void *data; 
		Node* next_data;
		void add_elements_to_the_node();
};

extern Node* head_pointer     
void Node :: add_elements_to_the_node()
{
	node *linked

	if (head_pointer == NULL)
	{
		head = node;
	}
}